import React from "react";

const PostPage = ({ pageContext }) => {
  console.log("### PostPage pageContext:", pageContext);

  return (
    <div>
      <span>PostPage</span>
    </div>
  );
};

export default PostPage;
